# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.pem

# Test binary, built with `go test -c`
*.test
bin/*

# gorelease output directory
dist/*

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Example terraform state
examples/.terraform/*
examples/*/*terraform*
*.terraform*

coverage*

terraform-provider-enos

# Don't include the enos flight control binaries in source. Generate them before
# compiling
internal/flightcontrol/binaries/*

# mirror
mirror/*.tfvars

.idea

# build dir for tests
test-build
