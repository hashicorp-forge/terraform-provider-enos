---
name: Release

on:
  push:
    branches:
      - main
      # - shain/badgenstuff # testing for main

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Build code
        run: CI='true' make release
